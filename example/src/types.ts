
export type ICat = {
  apiUrl: string;
  image: string;
  loading: boolean;
};

export type ICounter = {
  number: number;
  loading: boolean;
};

export type IMessage = {
  value: string;
  loading: boolean;
};